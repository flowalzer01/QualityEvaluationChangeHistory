<Window x:Class="QualityEvaluationChangeHistory.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:control="clr-namespace:QualityEvaluationChangeHistory.Controls"
        mc:Ignorable="d"
        DataContext="{Binding MainPageInstance, Source={StaticResource Locator}}"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="1*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <control:ValuesOverTimeChart Grid.Row="0" DataContext="{Binding ValuesOverTimeViewModel}"/>

        <ListView Grid.Row="1" Margin="10" Name="lvUsers" ItemsSource="{Binding FileChangeFrequencies}" ScrollViewer.CanContentScroll="True">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="File Pfad" DisplayMemberBinding="{Binding FilePath}" />
                    <GridViewColumn Header="Änderungsfrequenz" DisplayMemberBinding="{Binding FileChanges}" />
                </GridView>
            </ListView.View>
        </ListView>

    </Grid>
</Window>
