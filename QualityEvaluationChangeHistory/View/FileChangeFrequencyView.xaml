<UserControl x:Class="QualityEvaluationChangeHistory.View.FileChangeFrequencyView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:control="clr-namespace:QualityEvaluationChangeHistory.Controls"
             mc:Ignorable="d">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="1*"/>
        </Grid.RowDefinitions>

        <control:FileChangeFrequencyColumnChart Grid.Row="0" DataContext="{Binding FileChangeFrequencyColumnChartViewModel}"/>
        <ListView Grid.Row="1" Margin="10" Name="lvUsers" ItemsSource="{Binding FileChangeFrequencies}" ScrollViewer.CanContentScroll="True">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="File Path" DisplayMemberBinding="{Binding FilePath}" />
                    <GridViewColumn Header="File Change Frequency" DisplayMemberBinding="{Binding FileChanges}" />
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
